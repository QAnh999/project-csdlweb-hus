# Docker + PostgreSQL + Node.js App

Ứng dụng mẫu kết hợp Docker, PostgreSQL và Node.js

## 🚀 Cách chạy ứng dụng

### 1. Clone repository
```bash
git clone https://github.com/daobich14/my-docker-postgres-app.git
cd my-docker-postgres-app